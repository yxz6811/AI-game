{
  "name": "@hackathon/duplex-voice",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "True full-duplex bridge: MiniCPM-o Realtime → Intent Bridge → AIRI/Minecraft tools",
  "scripts": {
    "dev": "tsx --env-file-if-exists=.env --env-file-if-exists=.env.local src/main.ts",
    "start": "tsx --env-file-if-exists=.env --env-file-if-exists=.env.local src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests/**/*.test.ts"
  },
  "dependencies": {
    "@proj-airi/server-sdk": "workspace:*",
    "nanoid": "^5.1.5",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
